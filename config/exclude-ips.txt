# Адреса, которые модуль НЕ заворачивает в dnscrypt-proxy (по одному в строке).
# Bootstrap-резолверы и netprobe_address из dnscrypt-proxy.toml добавляются сюда
# автоматически - здесь только то, что нужно исключить дополнительно,
# например DNSCrypt-сервер, работающий на порту 53.
# 9.9.9.9
# 2620:fe::fe
